version: "3.0"
services:    
    client:
        build: ./packages/client/
        ports:
            - "9090:9090"

    server:
        build: ./packages/server/
        ports:
            - "8080:8080"
        
        

#     db:
#         image: "mysql"
#         environment: 
#             MYSQL_ROOT_PASSWORD: password
#         volumes: 
#             - web-data/testdb
            
# volumes: 
#     web-data: